def reflexive_divides :
  cts.Term cts.star (reflexive.reflexive nat.nat divides.divides)
  :=
  x:(cts.Term cts.box nat.nat) =>
  quotient.quotient
    x
    x
    (S.S O.O)
    (rewrite_r.rewrite_r
       nat.nat
       (times.times x (S.S O.O))
       (__:(cts.Term cts.box nat.nat) => eq.eq nat.nat __ (times.times x (S.S O.O)))
       (refl.refl nat.nat (times.times x (S.S O.O)))
       x
       (times_n_1.times_n_1 x)).

