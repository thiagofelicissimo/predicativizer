def sym_eq_minus_body_O :  (l?2 : cts.Sort) ->
  cts.Term
    (cts.succ l?2)
    (leibniz.leibniz
       cts.zero
       l?2
       (cts.prod
          cts.zero
          cts.zero
          cts.zero
          cts.I
          nat.nat
          (x => nat.nat))
       (m:(cts.Term cts.zero nat.nat) => O.O)
       (minus_body.minus_body O.O))
  := l?2 =>
  sym_leibniz.sym_leibniz
    cts.zero
    cts.zero
    l?2
    (cts.prod
       cts.zero
       cts.zero
       cts.zero
       cts.I
       nat.nat
       (x => nat.nat))
    (minus_body.minus_body O.O)
    (m:(cts.Term cts.zero nat.nat) => O.O)
    (eq_minus_body_O.eq_minus_body_O cts.zero).

