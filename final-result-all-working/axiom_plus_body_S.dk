axiom_plus_body_S : 
  cts.Term
    cts.zero
    (cts.prod
       cts.zero
       cts.zero
       cts.zero
       cts.I
       nat.nat
       (x =>
        equal.equal
          cts.zero
          (cts.prod
             cts.zero
             cts.zero
             cts.zero
             cts.I
             nat.nat
             (x0 => nat.nat))
          (plus_body.plus_body (S.S x))
          (m:(cts.Term cts.zero nat.nat) => S.S (plus.plus x m)))).

