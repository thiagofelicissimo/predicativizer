def divides_n_n :  (l?0 : cts.Sort) ->
  cts.Term
    l?0
    (cts.prod
       cts.zero
       l?0
       l?0
       cts.I
       nat.nat
       (x => divides.divides l?0 x x))
  := l?0 =>
  n:(cts.Term cts.zero nat.nat) =>
  reflexive_divides.reflexive_divides l?0 n.

